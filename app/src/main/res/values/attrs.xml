<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="FixedScaledLayout">
        <attr name="fixedWidth" format="dimension" />
        <attr name="fixedHeight" format="dimension" />
    </declare-styleable>

    <declare-styleable name="Theme">
        <!-- ============== -->
        <!-- Generic styles -->
        <!-- ============== -->
        <eat-comment />

        <!-- Specifies that a theme has a light background with dark text on top.  -->
        <attr name="buttonColor" format="color" />
        <attr name="buttonRadius" format="dimension" />
        <attr name="buttonDrawableBackground" format="reference" />
    </declare-styleable>
</resources>

