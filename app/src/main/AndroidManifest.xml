<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="pl.pharmaway.sunfarm.sunfarmpresentation">

    <application
        android:allowBackup="false"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".RegulacjaCykliActivity" />
        <activity android:name=".PozytywnieWplywaNaNastrojActivity" />
        <activity android:name=".RedukujeObjawyBolesnegoMiesiaczkowaniaActivity" />
        <activity android:name=".RedukujeKrwawienieZOdstawieniaActivity" />
        <activity android:name=".WNiewielkiSposobWplywaNaMaseCialaActivity" />
        <activity android:name=".ZmniejszaLiczbeIncydentowBoluGlowyActivity" />
        <activity android:name=".RedukujeObjawyTradzikuActivity" />
        <activity android:name=".DorinSummaryActivity" />
        <activity android:name=".MadinetteSummaryActivity" />
        <activity android:name=".SummaryActivity" />
    </application>

</manifest>